# Video assembly — 1.0.3 pinned deliberately: the caption/mask handling in
# pipeline/assemble.py is written around this version's behaviour.
moviepy==1.0.3
imageio-ffmpeg

# HTTP + LLM
requests
openai

# Voice: Chatterbox TTS, runs locally on CPU
chatterbox-tts

# Word-level caption timestamps (runs locally on CPU)
whisper-timestamped

# Imaging / audio
Pillow
numpy
soundfile

# Config
python-dotenv
